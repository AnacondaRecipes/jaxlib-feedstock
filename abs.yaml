# the conda build parameters to use
build_parameters:
  - "--suppress-variables"
  - "--skip-existing"
  - "--error-overlinking"
  - "--variants \"{skip_cuda_prefect: True}\""

channels:
  - https://staging.continuum.io/prefect/fs/bazel-feedstock/pr21/e687eb6